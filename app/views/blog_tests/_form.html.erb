<%= form_with(model: blog_test, local: true) do |form| %>
  <% if blog_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_test.errors.count, "error") %> prohibited this blog_test from being saved:</h2>

      <ul>
      <% blog_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_test_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :blog_test_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
